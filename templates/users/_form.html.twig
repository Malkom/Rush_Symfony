{{ form_start(form) }}
        {{ form_errors(form) }}
<div class="mt-3">
    {{ form_label(form.username, 'Pseudo') }}
    {{ form_errors(form.username) }}
    {{ form_widget(form.username) }}
    {{ form_help(form.username) }}
</div>
<div class="mt-3">{{ form_row(form.plainPassword) }}</div>
<div class="mt-3">
    {{ form_label(form.birthdate, 'Date de naissance') }}
    {{ form_errors(form.birthdate) }}
    {{ form_widget(form.birthdate) }}
    {{ form_help(form.birthdate) }}
</div>
<div class="mt-3">{{ form_row(form.description) }}</div>

<div class="d-flex justify-content-between mb-3">
<button class="btn btn-secondary">Enregistrer</button>

{{ form_end(form) }}
