<form method="post" action="{{ path('lists_delete', {'id': list.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cette liste ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ list.id) }}">
    <button class="btn btn-danger"/> Effacer la liste</button>
</form>
