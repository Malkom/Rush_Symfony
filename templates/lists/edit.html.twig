{% extends 'base.html.twig' %}

{% block title %}Edition de la liste{% endblock %}

{% block body %}
    <div class="container w-50 border mt-3">
        <h1>Edition de la liste</h1>

        {{ include('lists/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-dark ml-5" href="{{ path('user_lists', {'id': app.user.id}) }}">Retour à la liste</a>

        {{ include('lists/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}